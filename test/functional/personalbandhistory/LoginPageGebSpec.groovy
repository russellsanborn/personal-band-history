package personalbandhistory

import geb.spock.GebSpec

class LoginPageGebSpec extends GebSpec {
	def "user can reach login page" () {
            when:
                def x = 1
            then:
                x == 1
        }
}

